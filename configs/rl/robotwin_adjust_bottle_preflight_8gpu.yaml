defaults:
  - _robotwin_8gpu_base
  - _self_

hydra:
  searchpath:
    - file://${oc.env:RLINF_CONFIG_DIR}

runner:
  max_epochs: 1
  max_steps: 1
  val_check_interval: -1
  save_interval: 1
  logger:
    experiment_name: adjust_bottle_integration_gate_8gpu
    logger_backends: [tensorboard]

algorithm:
  update_epoch: 1
